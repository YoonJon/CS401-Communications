package shared.enums;

public enum ResponseType {
    MESSAGE,
    READ_UPDATED,
    REGISTER_RESULT,
    LOGIN_RESULT,
    LOGOUT_RESULT,
    CONVERSATION,
    CONVERSATION_METADATA,
    ADMIN_CONVERSATION_RESULT,
    LEAVE_RESULT,
    PONG,
    CONNECTED
}
